# Пет-проект MyKanban

Сайт, где создав проект, в нем можно создать ToDo список и/или kanban доски.

## Установка
В терминале
1. ```composer install```

2. Переименовть ```.env.example``` в ```env```

3. ```php artisan key:generate```

## Использование
```php artisan serve```
